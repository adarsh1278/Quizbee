
"use client";

import { useParams } from "next/navigation";

export default function Page() {
    const params = useParams();
    //uswffct ot check if it has joinded that room uewebsocekt store if joined then only proocedd or show a laoding screen if any show message loading screeen joineing room spiiner 

    return (
        <div className="bg-blue-300 h-14 mt-6 w-3xl">
            //question will be apper and timer slider will be in top which goes one ledft id question is asnwered then here your rank will be shown
        // live ldeaderboard shown inrifght side  in mobile bottom 

        </div>
    );
}
